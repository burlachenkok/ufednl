/** @file
* Version autogenerated file. Be aware that file uses inline variables (added only in C++17)
*/
#pragma once

namespace dopt
{
    /********************************************************************************
    * Manual specified information
    ********************************************************************************/

    /** Version
    */
    static inline const char* projectVersion = "22.08";

    /********************************************************************************
    * Gathered information from Git
    ********************************************************************************/
    
    /** Date of last change
    */
    static inline const char* gitlastChangeDate = "${DOPT_LAST_CHANGE_DATE}";
   
    /** Number of commit of the last change
    */
    static inline const char* gitLastChange = "${DOPT_LAST_CHANGE}";

    /** Date of previous change (Obsolete)
    * static inline const char* gitPreviousChange = "${DOPT_PREVIOUS_CHANGE}";
    */

    /** Current branch
    */
    static inline const char* gitBranch = "${DOPT_BRANCH_NAME}";

    /** Root directory for the project. It can be usefull for unittests.
    */
    static inline const char* gitRemoteRepository = "${DOPT_DEFAULT_REPOSITORY_URL}";

    /** Root directory for the project. It can be usefull for unittests.
    */
    static inline const char* projectRootDirectory4Build = "${DOPT_PROJECT_ROOT}";

    /** Build data and time
    */
    static inline const char* buildTimeStamp =  __DATE__ "/" __TIME__;

    /********************************************************************************
    * Gathered information from CMake
    ********************************************************************************/

    /** Full path for used C compiler to build sources
    */
    static inline const char* cmakeToolchainCCompiler = "${CMAKE_C_COMPILER}";

    /** Full path for used C++ compiler to build sources
    */
    static inline const char* cmakeToolchainCppCompiler = "${CMAKE_CXX_COMPILER}";

    /** Composite name of operating system CMake is compiling for
    */
    static inline const char* cmakeOSName = "${CMAKE_SYSTEM}";

    /** Target architecture of CPU for which binaries are building
    */
    static inline const char* cmakeTargetCPUArch = "${CMAKE_SYSTEM_PROCESSOR}";

    /** Compilation flags to be used when compiling CXX (C++) files
    */
    static inline const char* cmakeCompilerCppBuildFlags = "${CMAKE_CXX_FLAGS}";

    /** Linker flags to be used to create executables
    */
    static inline const char* cmakeLinkerExecutableBuildFlags = "${CMAKE_EXE_LINKER_FLAGS}";

    /********************************************************************************
    * Gathered information from CMake
    ********************************************************************************/
    /** Full path for used CUDA Toolkit in case of using CUDA. Empty otherwise.
    */
    static inline const char* cmakeCudaToolkitPath = "${CUDA_TOOLKIT_ROOT_DIR}";
}
